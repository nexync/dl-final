## Experiments on Minikey Doorkey

### Dependencies

A `requirements.yml` is provided. Run the command `conda env create -f requirements.yml`.

### How to Train

Run `python train.py`. Default command line arguments are preset.